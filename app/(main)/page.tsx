// Route marker only — the layout (app/(main)/layout.tsx) renders the
// actual UI, reading "/" as the active page straight from the URL. See
// that layout's own doc comment for why.
export default function Home() {
  return null;
}
